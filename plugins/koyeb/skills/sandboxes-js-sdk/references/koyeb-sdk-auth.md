# Koyeb SDK Authentication

Use an API token for SDK authentication.

## Environment variable

Set the token in your environment before running your code:

- `KOYEB_API_TOKEN`

If the SDK supports explicit token configuration, pass the same token value directly to the client initializer.
