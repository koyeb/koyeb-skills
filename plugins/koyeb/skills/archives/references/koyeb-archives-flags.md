# Archives Command Flags Reference

Complete reference for all `koyeb archives` command flags.

## Create
```
koyeb archives create NAME [flags]
  --ignore-dir strings   Directories to ignore (default: .git, node_modules, vendor)
```
